import Vue from "vue";
import VueRouter from "vue-router";

Vue.use(VueRouter);

const router = new VueRouter({
  routes: [
    {
      // path: url 주소
      path: "",
      // component: url 주소로 갔을때 표시될 컴포넌트
      component: "",
    },
  ],
});
